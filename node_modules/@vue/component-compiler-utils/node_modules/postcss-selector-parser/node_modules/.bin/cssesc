#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../../../cssnano/node_modules/cssesc/bin/cssesc" "$@"
  ret=$?
else 
  node  "$basedir/../../../../../../cssnano/node_modules/cssesc/bin/cssesc" "$@"
  ret=$?
fi
exit $ret
